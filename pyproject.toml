[project]
name = "meg"
version = "0.0.1"
description = "MEG — ERP for SMEs and Creators"
requires-python = ">=3.10"

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
